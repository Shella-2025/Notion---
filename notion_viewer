<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Visitor Counter</title>

<style>
body{
  margin:0;
  font-family:-apple-system,BlinkMacSystemFont,sans-serif;
  background:#e6dfcf;
}

.card{
  width:260px;
  padding:16px;
  border-radius:14px;
  background:#e6dfcf;
}

.title{
  font-weight:700;
  font-size:18px;
  margin-bottom:6px;
}

.desc{
  font-size:13px;
  margin-bottom:14px;
}

.grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:10px 20px;
}

.label{
  font-size:12px;
  color:#444;
}

.value{
  font-size:20px;
  font-weight:700;
  color:#2f7d57;
}
</style>
</head>

<body>

<div class="card">
  <div class="title">👾 瀏覽人數</div>
  <div class="desc">感謝瀏覽，有需求請盡快與我聯繫！:D</div>

  <div class="grid">
    <div><div class="label">Today</div><div class="value" data-target="7">0</div></div>
    <div><div class="label">Yesterday</div><div class="value" data-target="2">0</div></div>
    <div><div class="label">This Week</div><div class="value" data-target="0">0</div></div>
    <div><div class="label">This Month</div><div class="value" data-target="12">0</div></div>
    <div><div class="label">This Year</div><div class="value" data-target="126">0</div></div>
    <div><div class="label">Total</div><div class="value" data-target="126">0</div></div>
  </div>
</div>

<script>
const counters = document.querySelectorAll('.value');

counters.forEach(counter => {
  const target = +counter.getAttribute('data-target');
  const duration = 800; // 動畫時間 (毫秒)
  const stepTime = 20;
  const steps = duration / stepTime;
  const increment = target / steps;

  let current = 0;

  const update = () => {
    current += increment;
    if(current < target){
      counter.innerText = Math.floor(current);
      setTimeout(update, stepTime);
    } else {
      counter.innerText = target;
    }
  };

  update();
});
</script>

</body>
</html>
